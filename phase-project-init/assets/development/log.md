# Bundle Log

Material changes to the structure or conventions of this knowledge bundle —
not a changelog for the project itself, which is what the phase history is for.

Newest entries at the bottom.

## YYYY-MM-DD — bundle initialized

Created the `development/` bundle with its design and phase-history layers, and
installed the schema and validator under `scripts/okf/`.
